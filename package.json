{
  "name": "ai-hub-of-ideas-and-tasks",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev:backend": "cd backend && uvicorn app.main:app --reload --host ${BACKEND_HOST:-0.0.0.0} --port ${BACKEND_PORT:-8000}",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "npm-run-all -p dev:*"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}

